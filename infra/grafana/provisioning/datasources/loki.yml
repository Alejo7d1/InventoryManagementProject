# Datasource Loki provisionado automáticamente al arrancar Grafana.
# El uid "loki" es referenciado por el dashboard de DCAVA.
apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    uid: loki
    url: http://loki:3100
    isDefault: true
    jsonData:
      maxLines: 1000

